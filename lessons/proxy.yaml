name: Proxy Test
url: proxytest
difficulty: 3
pages:

        - num: 1
          content: proxy.html
          completable: true
          routes:
                  - path: /proxy
                    action: $custom.validate_proxy()
                    success_if_true: true
